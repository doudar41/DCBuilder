using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Cinemachine;

public class CameraLauncher : MonoBehaviour
{
/*    [SerializeField] List<Camera> cameras;
    Camera mainCamera;
    [SerializeField] Camera secondaryCamera;
    [SerializeField] CinemachineVirtualCamera vcamera;
    [SerializeField] CinemachineBlend cinemachineBlend;
    private void OnValidate()
    {
        foreach (Camera cam in cameras)
        {
            cam.tag = "Untagged";
        }

        secondaryCamera.tag = "MainCamera";
        secondaryCamera.GetComponent<CinemachineBrain>().enabled = true;
        secondaryCamera.GetComponent<CinemachineBrain>().ManualUpdate();
        print("Main Camera: " + Camera.main.name);
        
    }
    private void Awake()
    {
        secondaryCamera.GetComponent<CinemachineBrain>().enabled = true;

        secondaryCamera.GetComponent<CinemachineBrain>().ActiveBlend = secondaryCamera.GetComponent<CinemachineBrain>().m_CustomBlends;
    }
*/

}
